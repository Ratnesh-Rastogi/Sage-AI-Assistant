"""
Notes routes — placeholder.

Per CLAUDE_BUILD_PROMPT.md phase plan, this module's endpoints are implemented
in a later phase (Agent Core / Memory / Productivity / Advanced Tools).
Not wired into the Phase 1 application router yet.
"""
from fastapi import APIRouter

router = APIRouter(prefix="/notes", tags=["notes"])
